<!DOCTYPE html>
html lang="zh-cn"
	head
		title - ${data.text}
		link rel="stylesheet" media="all" href="/css/kube.css"
		link rel="stylesheet" media="all" href="/css/main.css"
	body
		div class="main-shot"
			for(size in data.size)
				div class="shot-preview"
					img src="/upload/${data.sid}.${size}.jpg" alt="${size}px" class="shot-preview-image"
					p class="shot-preview-text" - ${size}px
					input type="text" value="${data.domain}/upload/${data.sid}.${size}.jpg" class="shot-preview-url"